DATABASE_URL=postgresql://postgres:samuel@localhost:5432/bankAPI
SECRET_KEY=QcBUWji5jJTPDDYuaS6kbe8WIIz3JGB36l-unwels0Q

# App environment
APP_ENV=development
ENFORCE_HTTPS=false

# Request signing
# If empty, signing falls back to SECRET_KEY (not recommended outside local dev)
BANK_SIGNING_SECRET=

# Encryption key management
# Set STRICT_KEY_MANAGEMENT=true in production-like mode.
STRICT_KEY_MANAGEMENT=false

# 32-byte base64 URL-safe master key for envelope encryption.
# Generate with Python:
# python -c "import secrets,base64; print(base64.urlsafe_b64encode(secrets.token_bytes(32)).decode())"
MASTER_KEY_B64=